/* engine.js */

var Engine = function(board) {
  this.board = board; 

  var start = function() {
    // TODO: Setup and call vroom once every interval, event-driven
  };

  var vroom = function() {
    // TODO
  };
};
